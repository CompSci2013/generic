/**
 * Automobile domain UI configuration barrel export.
 *
 * Exports all UI configurations for the automobile domain:
 * - Table column definitions
 * - Chart configurations
 * - Picker configurations
 * - Filter definitions
 *
 * These configurations wire the generic framework components to
 * work with automobile-specific data models.
 */
export * from './auto-table-config';
export * from './auto-chart-config';
export * from './auto-picker-config';
export * from './auto-filter-config';
